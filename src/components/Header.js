import PropTypes from "prop-types";
import Button from "./Button";

const Header = () => {
  return (
    <div className="Header">
      <h1>Resume Parser</h1>
    </div>
  );
};

export default Header;
